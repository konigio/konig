CREATE TABLE IF NOT EXISTS sofa.PartyTable (
   ID VARCHAR(2000) NOT NULL,
   email VARCHAR(2000), 
   birthDate DATE, 
   foundingDate DATE,
   Price DOUBLE (3,4),
   Count DOUBLE,
   Number INT,
   Name CHAR(35),
   Phone FLOAT,
   SerialNumber TINYINT(1)
);
