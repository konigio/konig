This folder contains the expected output from the generator.
We don't include every file that is generated for every test.

For instance, we only include the generated pom.xml file in the tabular-mapping test case.
The other pom.xml files are similar so there is no need to test more than one.